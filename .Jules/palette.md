
## 2024-05-18 - Added `aria-label` to Inputs
**Learning:** Found that relying on placeholders in inputs without explicit labels is inaccessible to screen readers. Adding `aria-label` keeps the minimalist visual design while remaining fully accessible.
**Action:** Always add `aria-label` to form fields that rely visually on placeholders, rather than introducing visual labels when trying to maintain minimal designs.
